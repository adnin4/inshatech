# Release guardrails

- Only `master` is a production candidate.
- Legacy PRs are closed; do not merge them wholesale.
- Missing live evidence blocks `LIVE_VERIFIED`.
- Provider absence remains `NOT_CONFIGURED`.
- Route/browser/runtime failures block release.
- Real customer evidence is required for full mission certification.
