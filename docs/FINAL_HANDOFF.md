# Final Handoff

Deployment authority is Cloudflare Pages native Git integration. Production verification must fail closed until exact `/api/version` SHA parity and real browser/surface smoke pass. No UI/UX, database, payment, or agent business logic is changed by this certification branch.