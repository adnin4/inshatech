# Live Release Rules

A production release is verified only when the public runtime reports the exact master SHA and the live browser certification passes. Skipped or unknown deployment status is never a successful release.