# Live runtime certification appendix

The canonical production release must be verified against the live Cloudflare Pages runtime. CI-only or local-only evidence is not sufficient for `LIVE_VERIFIED`.
