# Clean URL routing is intentionally disabled during functionality recovery.
# Cloudflare Pages clean-URL handling must not be combined with explicit 200 rewrites
# from /route -> /route.html because that can create redirect loops.
# Canonical page assets remain available at their explicit .html paths.
